Language used: Python

=>How To run:
Open the terminal and type : 
python k-means.py [arg1] [arg2]

where 
arg1 = number of clusters you want in output
arg2 = path of output file

NOTE : Based on talks with the professor, we have implemented the program as directed but with a little change. We don’t take the path of the input file, instead we read the data from the link that the professor has provided. We did this to learn how to cater dynamic changes in the data. Thus we have used only 2 parameters instead of 3 parameters.